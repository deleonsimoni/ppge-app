import { ImagePathComplementPipe } from './image-path-complement.pipe';

describe('ImagePathComplementPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathComplementPipe();
    expect(pipe).toBeTruthy();
  });
});
