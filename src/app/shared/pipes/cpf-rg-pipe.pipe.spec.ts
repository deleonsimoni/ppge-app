import { CpfRgPipePipe } from './cpf-rg-pipe.pipe';

describe('CpfRgPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CpfRgPipePipe();
    expect(pipe).toBeTruthy();
  });
});
